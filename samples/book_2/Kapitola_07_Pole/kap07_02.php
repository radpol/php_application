<?php
  $pole = array(10,20);
  // Vytvoøení pole, do kterého budou uložena dvì èísla: desítka
  // a dvacítka.
 
  $pocet = count($pole);
  // Funkce count zde zjišuje poèet údajù v poli $pole. Protože
  // pole $pole obsahuje dva údaje, funkce count vrátí dvojku.
  // Promìnná $pocet tedy bude obsahovat dvojku.
 
  echo $pocet;
  // Vypíše: 2
  // Vypsání poètu údajù v poli.
?>
